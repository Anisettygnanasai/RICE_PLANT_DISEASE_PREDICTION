services:
  - type: web
    name: rice-disease-api
    env: docker
    plan: free # Change this to starter/standard depending on model size and traffic needs
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
